# vscode-pdf-docs

A starter VS Code extension project for `vscode-pdf-docs`.

## Development container workspace

This repository includes a dev container configuration at `.devcontainer/devcontainer.json`.

To use it:

1. Open the repository in VS Code.
2. Run **Dev Containers: Reopen in Container**.
3. Start development with `npm run watch` or `F5`.
